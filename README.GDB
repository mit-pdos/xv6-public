1. run the command "make qemu-gdb"
2. open up another terminal window and run gdb
3. in the newly opened gdb window, run the command "source -s -v cmd1" and then "source -s -v cmd2"

After these three steps, gdb should be running fine in 64 bit mode.
