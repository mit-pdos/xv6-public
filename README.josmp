## To build and run xv6 on josmp:

make HW=josmp
make HW=josmp rsync

ssh root@pdos ed /tftpboot/josmp/pxelinux.cfg <<EOM
/^default /s/^/#/
/^# *default xv6/s/^# *//
wq
EOM

ssh root@josmp reboot
ipmitool -I lanplus -A MD5 -H josmpadm -U ADMIN power reset

---

## To restore booting into local Linux:

ssh root@pdos ed /tftpboot/josmp/pxelinux.cfg <<EOM
/^default /s/^/#/
/^# *default localboot/s/^# *//
wq
EOM

ipmitool -I lanplus -A MD5 -H josmpadm -U ADMIN power reset

