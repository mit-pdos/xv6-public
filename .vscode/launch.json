{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Launch(remote)",
            "program": "${workspaceRoot}/kernel",
            "cwd": "${workspaceRoot}",
            "remote": {
                "enabled": true,
                "address": ":28480",
                "mode": "remote",
                "execfile": "${workspaceRoot}/kernel"
            }
        }
    ]
}